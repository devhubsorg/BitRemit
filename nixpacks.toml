providers = []

[phases.setup]
nixPkgs = ["nodejs_20", "foundry"]

[phases.install]
cmds = ["npm install -g pnpm@9.15.4", "pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["cd packages/contracts && forge build", "node export-abis.js"]
